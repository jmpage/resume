Typeset using [AltaCV](https://github.com/liantze/AltaCV) v1.1.4.

# Prereqs

This requires xetex and the fontawesome package which can be installed in Ubuntu via:

``` shell
apt-get install texlive-xetex texlive-fonts-extra
```

# Building

```shell
xelatex jenpage.tex
```
